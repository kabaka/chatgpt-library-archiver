# Gallery UX & Accessibility Review

**Date:** 2026-03-01
**Reviewer:** Gallery UX Designer (agent)
**Scope:** `gallery_index.html` (template), `gallery.py` (generator), generated output (`gallery/index.html`, `page_1.html`, `page_2.html`), `metadata.json`
**Gallery size:** 1 169 images, 2.3 MB metadata, ~20 KB template

---

## Table of Contents

1. [Executive Summary](#executive-summary)
2. [Responsive Design](#1-responsive-design)
3. [Accessibility (WCAG 2.1)](#2-accessibility-wcag-21)
4. [Performance](#3-performance)
5. [Dark Mode](#4-dark-mode)
6. [Navigation & Filtering](#5-navigation--filtering)
7. [Lightbox / Viewer](#6-lightbox--viewer)
8. [Metadata Display](#7-metadata-display)
9. [Error States](#8-error-states)
10. [Browser Compatibility](#9-browser-compatibility)
11. [Single-File Architecture](#10-single-file-architecture)
12. [JavaScript Quality](#11-javascript-quality)
13. [Security](#12-security)
14. [Visual Design](#13-visual-design)
15. [Comparison with Gallery Skill Patterns](#comparison-with-gallery-skill-patterns)
16. [Prioritized Recommendations](#prioritized-recommendations)
17. [Cross-Review Contributors](#cross-review-contributors)

---

## Executive Summary

The gallery consists of **two distinct implementations**:

| | Modern (`index.html`) | Legacy (`page_*.html`) |
|---|---|---|
| **Rendering** | Client-side via `metadata.json` | Server-rendered static HTML |
| **Features** | Search (boolean logic), date filtering, size selector, lightbox, dark mode, lazy loading | Title search only, size selector, dark mode |
| **Thumbnails** | Uses `thumbs/{small,medium,large}/` | Uses full-size images for everything |
| **Pagination** | None (loads all 1 169 items) | Hardcoded multi-page split |
| **Generated by** | Current `gallery.py` | Unknown; no code generates these |

The modern `index.html` is significantly better but has **critical accessibility gaps** (no viewport meta tag, no focus trap in lightbox, XSS via `innerHTML`), **no pagination** for 1 169 items, and **missing semantic HTML**. The legacy pages are obsolete and should be removed.

**Security cross-review update:** The security auditor's cross-review validated the XSS vulnerability (J-1) and identified additional attack vectors not covered in the original review — specifically `href` attribute injection via `conversation_link`, attribute context breakout in `alt`/`data-src`/`href`, and the upstream metadata poisoning chain from external APIs through `metadata.json` into the DOM. These findings are incorporated in [Section 12: Security](#12-security) and the updated [Prioritized Recommendations](#prioritized-recommendations).

### Severity Guide

| Rating | Meaning |
|---|---|
| 🔴 **Critical** | Blocks core usability or violates WCAG A |
| 🟠 **High** | Significantly degrades experience or violates WCAG AA |
| 🟡 **Medium** | Noticeable quality issue, should fix |
| 🟢 **Low** | Minor polish or nice-to-have |

---

## 1. Responsive Design

### What works well

- **CSS Grid with `auto-fill`/`minmax`** (`gallery_index.html:44`) — the grid naturally reflows across sizes without explicit column counts. This is the recommended pattern per the gallery skill.
- **Three responsive breakpoints** at `960px` and `680px` (`gallery_index.html:231–296`) — the header toolbar restructures progressively, hiding the title on small screens and reflowing controls.
- **Size classes** (small 150px, medium 250px, large 400px, full) let users tune density.
- **Full-size mode** uses `flex-direction: column` for a vertical stream layout.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| R-1 | 🔴 Critical | **Missing `<meta name="viewport">` tag.** Without it, mobile browsers render at ~980px width and everything is tiny. The gallery is effectively unusable on mobile. | `gallery_index.html:1–6` |
| R-2 | 🟡 Medium | **Date inputs overflow on narrow screens.** The `min-width: 0` fix is applied but date inputs with native pickers can still be clipped below ~360px. | `gallery_index.html:280–286` |
| R-3 | 🟡 Medium | **Gallery title hidden on mobile but no alternative branding.** `h1` is `display: none` at ≤680px with no reduced heading, so users lose context of what page they're on. | `gallery_index.html:252` |
| R-4 | 🟢 Low | **No max-width constraint on desktop.** On ultra-wide monitors (>2560px), the grid stretches very wide with large gaps between cards. A `max-width: 2000px` on `.layout` would improve readability. | `gallery_index.html:37–40` |

---

## 2. Accessibility (WCAG 2.1)

### What works well

- `<html lang="en">` is set correctly.
- Search input has `aria-describedby="searchHelp"`.
- Date inputs have `aria-label` attributes.
- GitHub icon has `aria-hidden="true"` on the SVG and a `.sr-only` label.
- Reset button has a `.sr-only` span for screen readers.
- `.sr-only` class is correctly implemented (clip pattern).

### Issues

| # | Severity | Issue | WCAG | Location |
|---|---|---|---|---|
| A-1 | 🔴 Critical | **No skip-to-content link.** Keyboard users must tab through all toolbar controls to reach images. The skill checklist requires this. | 2.4.1 (A) | `gallery_index.html:327` (after `<body>`) |
| A-2 | 🔴 Critical | **Lightbox has no focus trap.** When the viewer opens, focus stays wherever it was. Tab key moves to elements behind the overlay. Screen reader users have no indication the viewer opened. | 2.4.3 (A), 2.1.1 (A) | `gallery_index.html:660–698` |
| A-3 | 🔴 Critical | **Lightbox viewer `<div id="viewer">` has no ARIA role or label.** Should be `role="dialog"` with `aria-modal="true"` and `aria-label`. | 4.1.2 (A) | `gallery_index.html:316–322` |
| A-4 | 🟠 High | **Dark mode toggle is a `<div>` not a `<button>`.** Not focusable by default, no keyboard activation. Uses `onclick` handler only.** Same for search help icon (uses `<span>` with `tabindex="0"` but no `role="button"` or keyboard handler). | 2.1.1 (A), 4.1.2 (A) | `gallery_index.html:365, 343–348` |
| A-5 | 🟠 High | **Image cards lack focus indicators.** The `<a class="thumb">` elements receive focus via Tab but have no visible `:focus` or `:focus-visible` styling. | 2.4.7 (AA) | `gallery_index.html:78–82` |
| A-6 | 🟠 High | **No visible focus indicator on controls.** The `.toggle`, `.size-select`, date inputs, and reset button lack `:focus-visible` styles. Browser defaults may or may not be visible depending on the theme. | 2.4.7 (AA) | CSS section |
| A-7 | 🟠 High | **Metadata overlay only appears on hover (`:hover .meta`).** Keyboard-focused cards don't show metadata. There is no `:focus-within` trigger. | 1.3.1 (A) | `gallery_index.html:100` |
| A-8 | 🟡 Medium | **No semantic landmark elements.** Gallery uses `<div class="layout">` not `<main>`. No `<nav>` for controls. The skill checklist requires `<article>`, `<figure>`, `<figcaption>`, `<nav>`. Only `<header>` is used correctly. | 1.3.1 (A) | `gallery_index.html:327–376` |
| A-9 | 🟡 Medium | **`select#sizeSelector` has no `<label>` element** — the "Select Image Size:" text is in a `<strong>` inside a div, not associated via `for`/`id`. | 1.3.1 (A) | `gallery_index.html:358–364` |
| A-10 | 🟡 Medium | **Images with no title get empty `alt=""`** — when `item.title` is empty/falsy, the alt text is an empty string. Should fall back to filename or a descriptive default. The skill checklist says "use title or filename." | 1.1.1 (A) | `gallery_index.html:443` |
| A-11 | 🟡 Medium | **Search help icon `?` tooltip is title-only.** Screen readers may not announce `title` attributes reliably. Needs `aria-label` or a visible tooltip mechanism. | 4.1.2 (A) | `gallery_index.html:343–348` |
| A-12 | 🟢 Low | **No announced live region for filter results.** When filtering changes visible images, screen reader users get no feedback on how many results matched. | 4.1.3 (AA) | JS `filterGallery()` |

---

## 3. Performance

### What works well

- **Intersection Observer lazy loading** (`gallery_index.html:407–415`) — images use `data-src` and are loaded via `IntersectionObserver` with a 200px root margin. Combined with `loading="lazy"` as a fallback.
- **Thumbnail size switching** — when changing grid size, thumbnails are swapped to the appropriately-sized version, avoiding loading full-size images in grid view.
- **`cache: 'no-store'` on metadata fetch** ensures fresh data on reload.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| P-1 | 🟠 High | **All 1 169 DOM nodes created on load with no pagination.** Every image card is appended to the DOM at startup. For 1 169 items, this creates ~4 700+ DOM elements (card + link + img + meta div each). On low-end devices, initial render will be slow and scrolling may jank. The skill mentions "client-side pagination" but none is implemented. | `gallery_index.html:416–458` |
| P-2 | 🟠 High | **2.3 MB `metadata.json` loaded in one fetch.** Each item has ~30–40 tags with long descriptions. For 1 169 items, this is a significant initial payload. No streaming, no pagination, no progressive loading. | `gallery_index.html:405` |
| P-3 | 🟡 Medium | **`filterGallery()` is not debounced.** The search input uses `oninput="filterGallery()"` which fires on every keystroke. The function iterates all 1 169 cards and runs a boolean parser. On slower devices this could cause input lag. Skill guidelines recommend 200–300ms debounce. | `gallery_index.html:340, 522–549` |
| P-4 | 🟡 Medium | **Cards built one-by-one with `gallery.appendChild(card)` in a loop** instead of batching via `DocumentFragment`. Each append triggers a potential reflow. | `gallery_index.html:458` |
| P-5 | 🟢 Low | **`sessionStorage` checked with `typeof` guard but called synchronously on every filter/size change.** Minor but could be wrapped in a try/catch for private browsing safety. | `gallery_index.html:527–530` |

---

## 4. Dark Mode

### What works well

- **System preference detection** via `@media (prefers-color-scheme: dark)` with the clever `body:not(.light)` selector — respects OS preference but allows manual override.
- **`sessionStorage` persistence** remembers theme choice across page navigations within the session.
- **CSS custom properties** (`--bg`, `--text`, `--meta-color`, `--control-bg`) make theming clean and maintainable. Matches skill patterns exactly.
- **Transition on background/color** for smooth theme switching.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| D-1 | 🟡 Medium | **Metadata overlay uses hardcoded `rgba(0,0,0,0.6)` background and `#fff` text** regardless of theme. Works in both themes visually, but in dark mode the black overlay on a dark card creates a low-contrast edge. Not a WCAG fail but aesthetically inconsistent. | `gallery_index.html:91–99` |
| D-2 | 🟡 Medium | **Lightbox overlay uses hardcoded `rgba(0,0,0,0.8)`** and `color: white` — doesn't use CSS variables. Fine visually but diverges from the theming architecture. | `gallery_index.html:300–314` |
| D-3 | 🟢 Low | **Toggle is text-based ("Toggle Dark Mode") with no icon.** A sun/moon icon would be more compact and recognizable, especially on mobile where the toggle is hidden at ≤680px (see `gallery_index.html:294`). | `gallery_index.html:365` |
| D-4 | 🟢 Low | **Dark mode toggle hidden on mobile** (`.controls .toggle { display: none }` at ≤680px). Mobile users cannot switch themes at all. | `gallery_index.html:294` |

---

## 5. Navigation & Filtering

### What works well

- **Boolean search parser** (`tokenize()` / `makeSearchFn()`) supports AND, OR, NOT, and parentheses. Searches both titles and tags. This is a genuinely powerful feature for a static gallery.
- **Date range filtering** with native date inputs.
- **Combined AND logic** between text search and date range.
- **Filter state persistence** via `sessionStorage` — filters survive page reload.
- **Reset button** clears all filters.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| N-1 | 🟠 High | **No pagination.** All 1 169 items render at once. Users must scroll through the entire collection. The skill documentation mentions pagination but it's not implemented in the modern gallery. | — |
| N-2 | 🟠 High | **No sort options.** Items are sorted by `created_at` descending (set in Python) but users cannot sort by title, date ascending, or tags. | `gallery.py:32–35` |
| N-3 | 🟡 Medium | **No result count shown.** After filtering, no indicator tells users how many images matched. | — |
| N-4 | 🟡 Medium | **Search help icon shows a tooltip on hover only.** No click-to-expand help text. The boolean search syntax (AND/OR/NOT/parentheses) is powerful but undiscoverable. | `gallery_index.html:343–348` |
| N-5 | 🟡 Medium | **Tag-based navigation missing.** Tags are displayed in the overlay but not clickable — clicking a tag should filter to that tag. With 1 169 richly-tagged items, tag navigation would be a high-value feature. | `gallery_index.html:438–439` |
| N-6 | 🟢 Low | **No keyboard shortcut for search focus** (e.g., `/` or `Ctrl+K`). | — |

---

## 6. Lightbox / Viewer

### What works well

- **Keyboard navigation**: Arrow keys for prev/next, Escape to close.
- **Touch/swipe support**: `touchstart`/`touchend` with a 50px threshold, horizontal detection (ignores vertical swipes).
- **Click-outside-to-close** behavior.
- **Ctrl/Cmd+click passes through** to open image in a new tab.
- **Visible indices tracking** — lightbox navigation skips filtered-out images.
- **Raw file link** for direct access to full-size image.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| L-1 | 🔴 Critical | **No focus management.** When viewer opens, focus is not moved into the viewer. When it closes, focus is not returned to the triggering element. Tab navigates behind the overlay. | `gallery_index.html:655–670` |
| L-2 | 🟠 High | **No ARIA attributes on viewer.** Missing `role="dialog"`, `aria-modal="true"`, `aria-label`. Screen readers don't know a modal is open. | `gallery_index.html:316–322` |
| L-3 | 🟠 High | **Viewer image alt text is empty on open** — set to `""` in HTML, only populated when `showViewerAt()` runs. Brief flash of empty alt. Also, `viewerImg.alt` uses `item.title` which may be empty. | `gallery_index.html:318, 661` |
| L-4 | 🟡 Medium | **No prev/next buttons visible.** Arrow key navigation exists but there are no on-screen arrows for mouse/touch users to discover. Only swipe works on mobile. | `gallery_index.html:316–322` |
| L-5 | 🟡 Medium | **No image counter** (e.g., "3 of 47"). Users don't know their position in the collection. | — |
| L-6 | 🟡 Medium | **No zoom capability.** Full-size images may exceed viewport but there's no pinch-to-zoom or scroll-to-zoom. | — |
| L-7 | 🟡 Medium | **No loading indicator.** When opening a full-size image (could be several MB), there's no spinner or progress feedback. | — |
| L-8 | 🟢 Low | **No preloading of adjacent images.** When viewing image N, images N-1 and N+1 could be prefetched for smoother navigation. | — |

---

## 7. Metadata Display

### What works well

- **Hover overlay** shows title, date, truncated tags (first 5), and conversation link.
- **Tags have a full-list tooltip** via `span.title` attribute on the `.tags` element.
- **Ellipsis indicator** (`…`) when tags are truncated beyond 5.
- **Small grid hides date/tags** to save space (`.gallery-small .meta .created, .gallery-small .meta .tags { display: none }`).

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| M-1 | 🟠 High | **Metadata only visible on hover.** Touch devices can't hover — metadata is invisible on mobile/tablet entirely. Keyboard focus doesn't trigger it either (no `:focus-within` rule). | `gallery_index.html:100` |
| M-2 | 🟡 Medium | **Items without titles show the raw ID** (e.g., `s_d4763c843c7081918c2fc9e06e2b8efa`) — this is functional but not user-friendly. In `page_2.html:4570–4585`, several items have empty titles and display hash IDs. | `gallery_index.html:444` |
| M-3 | 🟡 Medium | **No image dimensions displayed.** The metadata includes `width` and `height` but they're not shown anywhere. | — |
| M-4 | 🟢 Low | **Conversation link text is generic** ("View conversation"). Could show the conversation title or at least a truncated ID. | `gallery_index.html:447` |
| M-5 | 🟢 Low | **Date format is ISO-ish** (`2025-09-14 19:00:54`) — no timezone indicator, no relative dates ("3 months ago"). | `gallery_index.html:433–437` |

---

## 8. Error States

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| E-1 | 🟠 High | **No error handling on `fetch('metadata.json')`.** If the file is missing, corrupted, or the gallery is opened via `file://` protocol (where fetch fails), the gallery shows a blank page with no error message. | `gallery_index.html:405–406` |
| E-2 | 🟡 Medium | **No broken image fallback.** If a thumbnail or full-size image fails to load (404, corrupt file), the browser shows a broken image icon. No `onerror` handler provides a placeholder or removes the card. | `gallery_index.html:443` |
| E-3 | 🟡 Medium | **Empty gallery shows nothing.** When metadata is an empty array, the gallery container is simply empty — no "No images found" message. | — |
| E-4 | 🟡 Medium | **Filtered-to-zero shows nothing.** When search/date filters exclude all results, there's no "No matching images" message. | `filterGallery()` |
| E-5 | 🟢 Low | **`file://` protocol incompatibility.** `fetch()` doesn't work over `file://` in most browsers. Users who double-click `index.html` will see nothing. A `<noscript>` message and/or XMLHttpRequest fallback would help. | `gallery_index.html:405` |

---

## 9. Browser Compatibility

### Features used and their support

| Feature | Browser Support | Risk |
|---|---|---|
| CSS Grid (`auto-fill`, `minmax`) | All modern (Chrome 57+, FF 52+, Safari 10.1+) | 🟢 Low |
| CSS Custom Properties | All modern (Chrome 49+, FF 31+, Safari 9.1+) | 🟢 Low |
| `IntersectionObserver` | All modern (Chrome 58+, FF 55+, Safari 12.1+) | 🟢 Low |
| `fetch()` API | All modern (Chrome 42+, FF 39+, Safari 10.1+) | 🟢 Low |
| `loading="lazy"` (native) | Chrome 77+, FF 75+, Safari 15.4+ | 🟢 Low (IO fallback exists) |
| `prefers-color-scheme` | Chrome 76+, FF 67+, Safari 12.1+ | 🟢 Low |
| `sessionStorage` | Universal | 🟢 Low |
| `<input type="date">` | Chrome/Edge/FF/Safari all support, styling varies | 🟡 Medium (inconsistent UI) |
| ES6 arrow functions, `const`, template patterns | All modern | 🟢 Low |

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| B-1 | 🟡 Medium | **No `<meta name="viewport">` tag.** On mobile Safari and Chrome, this causes the page to render at desktop width. (Also filed as R-1.) | `<head>` |
| B-2 | 🟢 Low | **No `<!DOCTYPE html>` quirks mode risk.** Actually present — `<!DOCTYPE html>` is correctly declared. No issue here. | — |
| B-3 | 🟢 Low | **IE11 not supported** (CSS Grid, Custom Properties, fetch, etc.). Appropriate for 2026. | — |

Overall browser compatibility is **good** — the gallery targets a sensible modern baseline.

---

## 10. Single-File Architecture

### Strengths

- **Zero dependencies** — no CDN requests, no build tools, no frameworks. Opens instantly offline.
- **Simple deployment** — Python copies one file. No asset pipeline needed.
- **~20 KB template size** — very lightweight for the feature set.
- **Self-contained** — a user who receives the `gallery/` directory has everything needed.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| S-1 | 🟡 Medium | **Legacy `page_*.html` files are orphaned.** `page_1.html` (5 575 lines) and `page_2.html` (4 645 lines) are a completely different implementation with hardcoded inline images (full-size, no thumbnails), duplicate JavaScript, no lightbox, weaker filtering. They are NOT generated by the current `gallery.py` and appear to be legacy artifacts. They confuse the gallery structure and load full-size images directly, wasting bandwidth. | `gallery/page_1.html`, `gallery/page_2.html` |
| S-2 | 🟡 Medium | **CSS and JS are not minified.** At 739 lines / 20 KB uncompressed, this isn't critical, but a minified version could trim to ~12 KB. Since it's a single file, gzip/brotli compression would bring it to ~4 KB. | — |
| S-3 | 🟢 Low | **No cache-busting mechanism.** The `index.html` filename never changes. If regenerated, browser cache may serve a stale version. A query string or versioned filename would help. | `gallery.py:42` |
| S-4 | 🟢 Low | **No `<noscript>` fallback.** Without JavaScript, users see just the header and an empty grid. A `<noscript>` message would explain that JS is required. | — |

---

## 11. JavaScript Quality

### Strengths

- **Boolean search parser** is well-implemented — recursive descent with proper operator precedence (NOT > AND > OR).
- **Viewer data/index tracking** is clean — `viewerData[]` and `visibleIndices[]` correctly handle filtered sets.
- **Touch handling** distinguishes swipes from taps, respects both horizontal and vertical axes.
- **Modular functions** — `loadImages()`, `filterGallery()`, `changeSize()`, `toggleDarkMode()` are reasonably separated.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| J-1 | 🔴 Critical | **XSS via `innerHTML` with unsanitized metadata.** Title, tags, filenames, and URLs from `metadata.json` are concatenated into HTML strings and injected via `card.innerHTML`. A title like `<img src=x onerror=alert(1)>` executes JavaScript. Also identified in the security audit (H-1). The security cross-review confirmed the Critical rating and identified additional vectors (`href` injection, attribute context breakout) — see [Section 12: Security](#12-security) for full analysis and mitigation recommendations. | `gallery_index.html:441–450` |
| J-2 | 🟡 Medium | **Global scope pollution.** All functions (`loadImages`, `filterGallery`, `toggleDarkMode`, etc.) and variables (`viewerData`, `visibleIndices`, `currentIndex`) are in the global scope. An IIFE or module pattern would be cleaner. | Throughout `<script>` |
| J-3 | 🟡 Medium | **Inline event handlers.** `oninput="filterGallery()"`, `onchange="filterGallery()"`, `onclick="toggleDarkMode()"` — these require global function names and are harder to maintain. Should use `addEventListener`. | `gallery_index.html:340, 350, 351, 365` |
| J-4 | 🟡 Medium | **`sessionStorage` calls not wrapped in try/catch.** In Safari private browsing (older versions) and some security-restricted contexts, `sessionStorage` throws. The `typeof` guard doesn't catch quota/access exceptions. | Multiple locations |
| J-5 | 🟢 Low | **`loadImages().then(filterGallery)` — no `.catch()`.** If metadata loading fails, the error is silently swallowed. | `gallery_index.html:738` |
| J-6 | 🟢 Low | **Magic numbers.** Swipe threshold `50`, root margin `'200px 0px'`, tag limit `5` — these should be named constants for readability. | Various |

---

## 12. Security

*This section consolidates findings from the original review and the security auditor's cross-review, which provided targeted analysis of XSS vectors in the gallery template.*

### Threat Model

The gallery renders data from `metadata.json`, which is populated by two external sources with no intermediate sanitization:

```
ChatGPT API → incremental_downloader.py → metadata.json → gallery_index.html (DOM)
                                                ↑
OpenAI Vision API → tagger.py ─────────────────┘
```

Neither the downloader nor the tagger sanitizes API responses before writing to metadata. The gallery then injects metadata values directly into the DOM via `innerHTML`. A compromised or adversarial API response — or manual edits to `metadata.json` — can achieve arbitrary JavaScript execution in the gallery context. The cross-review correctly emphasizes that this is a **metadata poisoning chain**: the XSS risk compounds with implicit trust in two external API sources.

### Findings

| # | Severity | Issue | Location |
|---|---|---|---|
| SEC-1 | 🔴 Critical | **Stored XSS via `innerHTML`.** All metadata fields (`title`, `tags`, `filename`, `conversation_link`, `item.id`) are concatenated into an HTML string and set via `card.innerHTML` with no escaping. A title of `<img src=x onerror=alert(1)>` executes JavaScript. Same root cause as J-1; confirmed by the security cross-review. | `gallery_index.html:441–449` |
| SEC-2 | 🔴 Critical | **`href` attribute injection via `conversation_link`.** The conversation link is injected directly into `<a href="...">`. A `conversation_link` value of `javascript:alert(document.cookie)` would execute on click. The `escapeHtml()` approach recommended for SEC-1 does **not** mitigate this vector — URL scheme validation is required separately. *Identified by security cross-review.* | `gallery_index.html:447` |
| SEC-3 | 🟠 High | **Attribute context breakout via `alt`, `data-src`, `href`.** `title` is injected into `alt="' + title + '"` and `filename` is injected into multiple `data-thumb-*` and `href` attributes without escaping. A title like `" onfocus="alert(1)" tabindex="0` or a filename containing `" onload="alert(1)` breaks the attribute boundary and injects event handlers. *Identified by security cross-review.* | `gallery_index.html:442–443` |
| SEC-4 | 🟡 Medium | **Viewer uses unsanitized filenames for path construction.** The lightbox sets `img.src`, `img.alt`, and `raw.href` via DOM property assignment (safer than `innerHTML`), but `item.src` is `'images/' + item.filename`. A filename with path traversal characters (`../../../etc/passwd`) could request files outside the gallery directory. Practical impact is limited — no code execution, only unexpected path access. | `gallery_index.html:661–663` |
| SEC-5 | 🟢 Low | **No Content-Security-Policy meta tag.** The gallery has no CSP, offering no defense-in-depth against XSS even if escaping is imperfect. | `gallery_index.html:<head>` |

### Recommended Mitigations

**1. Replace `innerHTML` with `createElement()`/`textContent` (addresses SEC-1, SEC-3).**

The security cross-review recommends this approach over an `escapeHtml()` function, and I agree. An escape function must be applied correctly at *every* injection point and is fragile against future code changes. DOM API construction eliminates the entire class of text and attribute injection structurally:

```javascript
const link = document.createElement('a');
link.href = imgPath;
link.className = 'thumb';
const img = document.createElement('img');
img.dataset.src = thumbPath;
img.alt = title || item.filename || item.id;
img.loading = 'lazy';
link.appendChild(img);
card.appendChild(link);
```

**2. Validate URL schemes for `href` attributes (addresses SEC-2).**

The cross-review proposes a `safeHref()` function restricting to `http:` and `https:` protocols:

```javascript
function safeHref(url) {
  if (!url || url === '#') return '#';
  try {
    const parsed = new URL(url, location.href);
    if (['http:', 'https:'].includes(parsed.protocol)) return url;
  } catch (e) {}
  return '#';
}
```

> **Caveat — `file://` protocol:** When the gallery is opened locally via `file://`, relative paths like `images/photo.jpg` resolve to `file:///…/images/photo.jpg`. The `safeHref()` function above would block these since `file:` is not in the allowlist. **Apply `safeHref()` only to external URLs** (e.g., `conversation_link`), not to relative image paths. Image paths should be protected by filename sanitization instead (see below). This concern was noted in the UX cross-review.

**3. Sanitize filenames against path traversal (addresses SEC-4).**

```javascript
function safeFilename(name) {
  return (name || '').replace(/[\/\\]/g, '_');
}
const imgPath = 'images/' + safeFilename(item.filename);
```

**4. Add a Content-Security-Policy meta tag (addresses SEC-5).**

```html
<meta http-equiv="Content-Security-Policy"
      content="default-src 'self'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src 'self' data:;">
```

Defense-in-depth: even if an XSS payload bypasses escaping, it cannot load external scripts or exfiltrate data to external domains. `'unsafe-inline'` is required because the gallery uses an inline `<script>` tag — a future improvement would externalize the script and use a nonce or hash. Note that `'self'` may behave inconsistently when loaded via `file://` protocol (Chrome treats `file://` pages as having a null origin), so CSP primarily protects served galleries.

---

## 13. Visual Design

### Strengths

- **Clean, minimal aesthetic.** No unnecessary borders, shadows, or gradients. Cards are simple with rounded corners.
- **Consistent spacing** via `--space` and `--gap` custom properties.
- **Metadata overlay** is tastefully done — semi-transparent black with white text, truncated tags.
- **Good use of `object-fit: contain`** — images maintain aspect ratio without cropping.

### Issues

| # | Severity | Issue | Location |
|---|---|---|---|
| V-1 | 🟡 Medium | **Cards have no visual border or shadow** in light mode. On a white background, white images blend into the background with no clear card boundary. A subtle `box-shadow` or `border` would help. | `gallery_index.html:69–75` |
| V-2 | 🟡 Medium | **Typography is `sans-serif` only** — no specific font stack. This gives inconsistent rendering across OSes (Helvetica on macOS, Arial on Windows, DejaVu on Linux). A system font stack (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, ...`) would be more polished. | `gallery_index.html:20` |
| V-3 | 🟢 Low | **Gallery title "Image Gallery" is generic.** Could be customizable or show the collection name. | `gallery_index.html:5, 329` |
| V-4 | 🟢 Low | **Reset button uses Unicode `⟲` character** which renders differently across systems. An inline SVG would be more consistent. | `gallery_index.html:352` |
| V-5 | 🟢 Low | **Lightbox has no transition animation.** Appears/disappears instantly (`display: none` → `display: flex`). A fade-in would feel smoother. | `gallery_index.html:300` |

---

## Comparison with Gallery Skill Patterns

Evaluating the current implementation against the patterns defined in `.github/skills/gallery-html-patterns/SKILL.md`:

| Skill Requirement | Status | Notes |
|---|---|---|
| Single bundled HTML file | ✅ | Correctly implemented |
| Reads `metadata.json` at runtime | ✅ | Via `fetch()` |
| CSS custom properties for theming | ✅ | `--bg`, `--text`, `--meta-color`, `--control-bg` |
| Responsive grid with `auto-fill`/`minmax` | ✅ | Exactly as prescribed |
| Size classes (small/medium/large/full) | ✅ | All four implemented |
| Dark mode via `.dark` class toggle | ✅ | Plus system preference detection |
| All images have `alt` text | ⚠️ Partial | Empty alt when title is missing |
| Visible focus indicators | ❌ Missing | No `:focus-visible` styles |
| ARIA labels on controls | ⚠️ Partial | Date inputs have labels; toggle, size selector lack them |
| Focus trap in lightbox | ❌ Missing | No focus management at all |
| Escape closes lightbox | ✅ | Implemented |
| Color contrast WCAG AA (4.5:1) | ⚠️ Uncertain | Light mode: `#000` on `#fff` ✅; Dark mode: `#eee` on `#111` ✅; Meta text: `#444` on `#fff` is 9.7:1 ✅; Control bg `#ddd` — text on `#ddd` depends |
| Keyboard-only navigation | ⚠️ Partial | Tab works for links; toggle div not keyboard-accessible |
| Skip-to-content link | ❌ Missing | Not implemented |
| Semantic HTML (`article`, `nav`, `figure`) | ❌ Missing | Only `<header>` used |
| Thumbnail selection per view | ✅ | Grid→small, detail→medium, lightbox→full |
| Lazy loading via `loading="lazy"` | ✅ | Plus IntersectionObserver |
| Debounce filter inputs | ❌ Missing | Direct `oninput` handler |
| Batch DOM updates (DocumentFragment) | ❌ Missing | Individual `appendChild` calls |
| Client-side pagination | ❌ Missing | All items rendered at once |
| XSS-safe rendering (no raw `innerHTML`) | ❌ Missing | All metadata injected via `innerHTML` unsanitized |
| URL scheme validation for `href` | ❌ Missing | `conversation_link` injected directly into `href` |
| CSP meta tag | ❌ Missing | No Content-Security-Policy |

**Compliance: 10/22 fully met, 4/22 partial, 8/22 missing.**

---

## Prioritized Recommendations

### P0 — Critical (fix immediately)

1. **Add `<meta name="viewport" content="width=device-width, initial-scale=1">`** to `<head>`. Without this, the gallery is broken on every mobile device. [R-1, B-1]

2. **Eliminate XSS by replacing `innerHTML` with `createElement()`/`textContent`.** Refactor card construction to use DOM API methods instead of string concatenation. This structurally prevents stored XSS (SEC-1) and attribute context breakout (SEC-3). An `escapeHtml()` function is fragile and does not protect `href` attributes. The security cross-review strongly recommends this approach. [J-1, SEC-1, SEC-3]

3. **Add URL scheme validation for `conversation_link` href.** Apply a `safeHref()` validator to reject `javascript:` and other dangerous URL schemes on external links. Apply only to external URLs — restricting to `http:`/`https:` would break image links when the gallery is opened via `file://` protocol. [SEC-2]

4. **Add a Content-Security-Policy meta tag** as defense-in-depth: `default-src 'self'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src 'self' data:;`. [SEC-5]

5. **Add focus management to the lightbox.** On open: move focus to viewer, set `role="dialog"`, `aria-modal="true"`, trap Tab within. On close: return focus to the card that triggered it. [A-2, A-3, L-1, L-2]

6. **Add a skip-to-content link** as the first focusable element in `<body>`. [A-1]

### P1 — High (fix soon)

7. **Implement client-side pagination** (e.g., 100 items per page, infinite scroll or page buttons). For 1 169 items, this dramatically improves initial load time and scroll performance. [P-1, N-1]

8. **Sanitize filenames against path traversal.** Strip `/` and `\` from `item.filename` before constructing image paths to prevent directory traversal. [SEC-4]

9. **Make all controls keyboard-accessible.** Change the dark mode toggle from `<div>` to `<button>`. Add `role="button"` and `keydown` handler to the help icon. [A-4]

10. **Add `:focus-visible` styles** for all interactive elements (links, buttons, inputs, select). [A-5, A-6]

11. **Make metadata accessible without hover.** Add `:focus-within .meta { display: block }` CSS rule. Consider an always-visible title below the image in list/full views. [A-7, M-1]

12. **Handle fetch errors gracefully.** Wrap `loadImages()` in try/catch, show a user-friendly error `div` in the gallery container. Handle `file://` protocol. [E-1, E-5]

13. **Add sort controls** (date ↑↓, title A-Z). [N-2]

### P2 — Medium (quality improvements)

14. **Debounce `filterGallery()`** with a 200ms delay on the search input. [P-3]

15. **Batch DOM construction** using `DocumentFragment`. [P-4]

16. **Add semantic HTML** — wrap gallery in `<main>`, controls in `<nav>`, cards in `<article>` or `<figure>`. [A-8]

17. **Associate `<label>` with `sizeSelector`** `<select>`. [A-9]

18. **Fall back to filename for empty alt text.** [A-10]

19. **Show a result count** after filtering (e.g., "Showing 47 of 1 169 images"). [N-3, A-12]

20. **Add clickable tags** that pre-fill the search. [N-5]

21. **Add card borders/shadows** in light mode. [V-1]

22. **Use a system font stack.** [V-2]

23. **Show "No images found" message** when gallery is empty or filtered to zero. [E-3, E-4]

24. **Add `onerror` fallback** for broken images. [E-2]

25. **Remove legacy `page_*.html` files** or add them to `.gitignore`. [S-1]

26. **Add visible prev/next buttons in lightbox.** [L-4]

27. **Add image counter in lightbox** ("3 of 47"). [L-5]

### P3 — Low (polish)

28. Fix mobile dark mode toggle visibility. [D-4]
29. Add a loading indicator for lightbox images. [L-7]
30. Preload adjacent images in lightbox. [L-8]
31. Add `<noscript>` fallback message. [S-4]
32. Add lightbox open/close transitions. [V-5]
33. Use system font stack and SVG reset icon. [V-2, V-4]
34. Add search keyboard shortcut (`/`). [N-6]
35. Max-width constraint for ultra-wide displays. [R-4]
36. Wrap `sessionStorage` in try/catch. [J-4]
37. Wrap all JS in an IIFE to avoid global pollution. [J-2]
38. Add `.catch()` to `loadImages()` promise. [J-5]

---

## Appendix: File Inventory

| File | Lines | Size | Role |
|---|---|---|---|
| `src/.../gallery_index.html` | 739 | 20 KB | Template (source of truth) |
| `gallery/index.html` | 739 | 20 KB | Deployed copy (identical to template) |
| `gallery/metadata.json` | 66 055 | 2.3 MB | Metadata for 1 169 images |
| `gallery/page_1.html` | 5 575 | ~200 KB | Legacy static page (orphaned) |
| `gallery/page_2.html` | 4 645 | ~165 KB | Legacy static page (orphaned) |
| `src/.../gallery.py` | 50 | ~1.4 KB | Generator script |

---

*Total findings: 6 critical, 13 high, 20 medium, 15 low (including security cross-review additions). Of these, the viewport meta tag, XSS elimination (including `href` injection and attribute context escaping), CSP hardening, and lightbox focus management are the most impactful to address first.*

---

## Cross-Review Contributors

| Contributor | Role | Key Contributions |
|---|---|---|
| **Security Auditor** (cross-review) | Security analysis of gallery XSS vectors | Validated `innerHTML` XSS as Critical (agreed with J-1). Identified `href` attribute injection via `conversation_link` (SEC-2) and attribute context breakout in `alt`/`data-src` (SEC-3) as missed vectors. Recommended `createElement()`/`textContent` over `escapeHtml()` as structurally safer. Proposed URL scheme validation with `safeHref()`, filename sanitization against path traversal, and CSP meta tag for defense-in-depth. Mapped the metadata poisoning chain from external APIs through `metadata.json` into the gallery DOM. Noted that the original review underplayed severity by placing XSS under "JavaScript Quality" rather than a dedicated security section. |

**Source:** `docs/reviews/cross-review-security-perspective.md`, Section 1 (Gallery UX & Accessibility Review — XSS Analysis).

**Caveats noted during incorporation:**
- The `safeHref()` URL scheme allowlist (`http:`, `https:` only) would break relative image paths when the gallery is opened via `file://` protocol. It should only be applied to external URLs like `conversation_link`, not to image `href` attributes.
- CSP `'self'` behaves inconsistently under `file://` protocol (null origin in Chrome), so the CSP meta tag is defense-in-depth for served galleries rather than a universal protection.
